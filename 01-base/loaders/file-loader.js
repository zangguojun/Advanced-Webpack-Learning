/**
 * loader.raw
 * true: loader得到的是一个二进制Buffer
 * false: loader得到的是一个UTF-8的字符串
 */
function loader(source) {
  // 1、先生成一个文件名
  let filename = 'aaa.png'
  // 2、向输出目录写入一个文件 this.emitFile(filename)

  // 3、返回一个脚本

}